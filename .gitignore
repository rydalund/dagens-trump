# Node.js - Ignore the 'node_modules' directory that contains all installed dependencies
node_modules/

# npm & Yarn logs - Ignore log files created during npm or Yarn operations (e.g., if there are errors during install)
npm-debug.log*
yarn-debug.log*

# Environment variables - Ignore '.env' file which may contain sensitive information (e.g., API keys or database credentials)
.env

# Log files - Ignore log files that may be generated during app runtime (e.g., server logs)
*.log

# Build directories - Ignore directories where compiled or bundled files are stored (e.g., Webpack, Babel, or TypeScript outputs)
dist/
build/

# TypeScript build info - Ignore files related to TypeScript build info
*.tsbuildinfo

# Babel configuration - Ignore Babel config files if you're using Babel for JS transpiling
.babelrc

# IDE-specific files - Ignore configuration files created by IDEs (e.g., VSCode or IntelliJ)
.vscode/
.idea/

# Operating system-specific files - Ignore files automatically created by the OS (e.g., macOS and Windows system files)
.DS_Store
Thumbs.db

# Additional log files - Ignore npm debug logs and other logs that are generated when npm fails
npm-debug.log*